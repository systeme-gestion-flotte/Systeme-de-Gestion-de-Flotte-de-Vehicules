services:
  # 1. Jaeger - Distributed Tracing
  jaeger:
    image: jaegertracing/all-in-one:1.55
    container_name: flotte-jaeger
    ports:
      - "16686:16686" # Web UI
      - "4317" # OTLP gRPC (internal use)
    restart: unless-stopped

  # 2. Prometheus - Metrics
  prometheus:
    image: prom/prometheus:v2.51.0
    container_name: flotte-prometheus
    volumes:
      - ./infrastructure/docker-observability/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    restart: unless-stopped

  # 3. Loki - Log Aggregation
  loki:
    image: grafana/loki:2.9.4
    container_name: flotte-loki
    command: -config.file=/etc/loki/local-config.yaml
    volumes:
      - ./infrastructure/docker-observability/loki.yaml:/etc/loki/local-config.yaml
    ports:
      - "3100:3100"
    restart: unless-stopped

  # 4. Grafana - Dashboard Visualization
  grafana:
    image: grafana/grafana:10.4.1
    container_name: flotte-grafana
    volumes:
      - ./infrastructure/docker-observability/grafana-datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml
    ports:
      - "3101:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - prometheus
      - loki
      - jaeger
    restart: unless-stopped

  # 5. OpenTelemetry Collector - Central Ingestion
  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.96.0
    container_name: flotte-otel-collector
    command: ["--config=/etc/otel-collector-config.yaml"]
    volumes:
      - ./infrastructure/docker-observability/otel-collector-config.yaml:/etc/otel-collector-config.yaml
    ports:
      - "4317:4317" # OTLP gRPC (le port sur lequel Spring Boot enverra les données)
      - "4318:4318" # OTLP HTTP
      - "8889:8889" # Prometheus exporter metrics
    depends_on:
      - jaeger
      - prometheus
      - loki
    restart: unless-stopped
