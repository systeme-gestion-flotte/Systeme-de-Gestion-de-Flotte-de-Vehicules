apiVersion: v1
kind: Service
metadata:
  name: maintenance-service
  namespace: fleet-management
spec:
  selector:
    app: maintenance-service
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP
