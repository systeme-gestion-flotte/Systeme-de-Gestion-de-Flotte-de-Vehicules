apiVersion: v1
kind: Service
metadata:
  name: vehicule-service
  namespace: fleet-management
spec:
  selector:
    app: vehicule-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP