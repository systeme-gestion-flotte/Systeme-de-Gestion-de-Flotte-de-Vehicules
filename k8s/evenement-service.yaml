apiVersion: v1
kind: Service
metadata:
  name: evenement-service
  namespace: fleet-management
spec:
  selector:
    app: evenement-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP