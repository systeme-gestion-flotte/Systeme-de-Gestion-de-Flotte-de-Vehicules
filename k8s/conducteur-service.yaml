apiVersion: v1
kind: Service
metadata:
  name: conducteur-service
  namespace: fleet-management
spec:
  selector:
    app: conducteur-service
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP