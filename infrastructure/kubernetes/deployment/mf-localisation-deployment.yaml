apiVersion: apps/v1
kind: Deployment
metadata:
  name: mf-localisation
  namespace: fleet-management
  labels:
    app: mf-localisation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mf-localisation
  template:
    metadata:
      labels:
        app: mf-localisation
    spec:
      containers:
        - name: mf-localisation
          image: leiti20/mf-localisation:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
