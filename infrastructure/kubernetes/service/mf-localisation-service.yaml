apiVersion: v1
kind: Service
metadata:
  name: mf-localisation
  namespace: fleet-management
  labels:
    app: mf-localisation
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: mf-localisation
