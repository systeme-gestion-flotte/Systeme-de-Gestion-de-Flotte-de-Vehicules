apiVersion: v1
kind: Service
metadata:
  name: mf-evenement
  namespace: fleet-management
  labels:
    app: mf-evenement
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: mf-evenement
