apiVersion: v1
kind: Service
metadata:
  name: mf-maintenance
  namespace: fleet-management
  labels:
    app: mf-maintenance
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: mf-maintenance
